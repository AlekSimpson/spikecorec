<neuroml xmlns="http://www.neuroml.org/schema/neuroml2" id="Glif3RingNetworkTop">
  <!--
    Thin include only wrapper, matching examples/glif_torus_network.h's own load_generated_model
    pattern: NML_Parser::parse XSD validates only the TOP LEVEL file it's given, and a raw LEMS
    ComponentType declaration (glif3_ring_network.nml's own GLIF3Cell) does not validate against
    the NeuroML schema directly.
  -->
  <include href="glif3_ring_network.nml"/>
</neuroml>
